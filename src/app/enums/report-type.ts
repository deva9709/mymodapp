export enum ReportType {
    TrainerEngagement = 1,
    TrendingSessions = 2,
    TrainerOnlineHours = 3
}

export enum ChartType {
    pie = 1,
    bar = 2
}

export enum DownloadReportType {
    TrainerSession = 0,
    TrainerList = 1,
    TraineeManualAttendance=2
}